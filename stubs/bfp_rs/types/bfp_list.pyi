class BfpList:
    """
    A list class with extra typing metadata that supports (mostly) the same API as the default list class. This is only useful
    for runtime type checks, and should not be used for type hinting
    """
